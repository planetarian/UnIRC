<Page
    x:Class="UnIRC.Views.NetworksView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:UnIRC"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    DataContext="{Binding Networks, Source={StaticResource Locator}}">
    <Page.Resources>
        <Style TargetType="Button" x:Key="IconButton">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="FontFamily" Value="Segoe MDL2 Assets"/>
            <Setter Property="Margin" Value="5"/>
        </Style>
    </Page.Resources>
    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}" Padding="10">
        <StackPanel>
            <TextBlock Text="Network:"/>
            <StackPanel
                Padding="0,10"
                Orientation="Horizontal"
                Visibility="{Binding AddingNewNetwork,
                Converter={StaticResource IsFalseVisibilityConverter}}">
                <ComboBox ItemsSource="{Binding Networks}" SelectedItem="{Binding SelectedNetwork}" DisplayMemberPath="Network.Name" Width="200"/>
                <Button Style="{StaticResource IconButton}" Content="&#xE710;" Command="{Binding CreateNewNetworkCommand}" />
                <Button Style="{StaticResource IconButton}" Content="&#xE70F;" Command="{Binding EditNetworkCommand}" />
                <Button Style="{StaticResource IconButton}" Content="&#xE711;" Command="{Binding DeleteNetworkCommand}"/>
            </StackPanel>
            <Grid
                Margin="0,10"
                Background="{ThemeResource SystemControlBackgroundBaseLowBrush}"
                Visibility="{Binding AddingNewNetwork,
                Converter={StaticResource IsTrueVisibilityConverter}}"
                HorizontalAlignment="Left">
                <StackPanel Orientation="Horizontal" Padding="10">
                    <TextBlock Text="New network name:" VerticalAlignment="Center" Padding="0,0,10,0"/>
                    <TextBox Text="{Binding NewNetworkName, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Width="150"/>
                    <Button Style="{StaticResource IconButton}" Content="&#xE710;" Command="{Binding SaveNewNetworkCommand}" />
                </StackPanel>
            </Grid>
            <StackPanel
                Visibility="{Binding SelectedNetwork,
                Converter={StaticResource IsNotNullVisibilityConverter}}">
                <TextBlock Text="Server:"/>
                <StackPanel
                    Orientation="Horizontal"
                    DataContext="{Binding SelectedNetwork}">
                    <ComboBox ItemsSource="{Binding Servers}" SelectedItem="{Binding SelectedServer}" Width="200"/>
                    <Button Style="{StaticResource IconButton}" Content="&#xE710;" Command="{Binding NewServerCommand}" />
                    <Button Style="{StaticResource IconButton}" Content="&#xE70F;" Command="{Binding EditServerCommand}" />
                    <Button Style="{StaticResource IconButton}" Content="&#xE711;" Command="{Binding DeleteServerCommand}"/>
                </StackPanel>
            </StackPanel>
        </StackPanel>
    </Grid>
</Page>